# Rdao Hardhat Project

This project is for a challenge

Try running some of the following tasks:

We need to add some private key on .env following explaple env.example

```shell
yarn
npx hardhat compile
npx hardhat test
yarn hardhat deploy --network goerli
```
